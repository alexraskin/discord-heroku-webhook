# discord-heroku-webhook

A simple web server, it listens for webhooks (specifically heroku) and posts the build status to discord

### Install
`pipenv install`

### Start
`pipenv run server`

### Verify
Open `http://127.0.0.1:5000`